Ficheros SQL que se cargarán al levantar Docker.

En caso de no usar Docker, se pueden importar a mano.

Listado de ficheros:

- basic_tables.sql: tablas básicas necesarias para el funcionamiento normal del framework.
- sales_example.sql_ (renombrar para usar): ejemplo de BDD con algunos datos para pruebas.